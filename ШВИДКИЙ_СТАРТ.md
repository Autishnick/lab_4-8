# 🚀 Швидкий Старт Music System

## ✅ JavaMail Підключено!

JavaMail успішно додано до проекту. Email розсилка тепер працює! 📧

---

## 🎯 Команди для Запуску

### 1️⃣ Запуск Програми (з email)

```bash
./run.sh
```

**Що робить:**
- ✅ Компілює проект з JavaMail
- ✅ Запускає програму
- ✅ Email розсилка працює

---

### 2️⃣ Тільки Компіляція

```bash
./compile.sh
```

Компілює весь проект без запуску.

---

### 3️⃣ Запуск Всіх Тестів

```bash
./test_all.sh
```

**Перевіряє:**
- ✅ Юніт тести
- ✅ Логування у файл
- ✅ Email розсилку

---

### 4️⃣ Швидкий Тест Email

```bash
javac -cp "libs/javax.mail-1.6.2.jar" -d bin -encoding UTF-8 -sourcepath src src/com/musicsystem/TestEmailAndLogging.java
java -cp "bin:libs/javax.mail-1.6.2.jar" com.musicsystem.TestEmailAndLogging
```

Відправить тестовий email (якщо налаштували app password).

---

## ⚙️ Налаштування Email (якщо ще не зробили)

### Крок 1: App Password

1. Відкрийте: https://myaccount.google.com/apppasswords
2. Увійдіть як: `vladisalvpro100igrok@gmail.com`
3. Згенеруйте app password
4. Скопіюйте його

### Крок 2: Додайте в Конфігурацію

Відкрийте: `resources/logging.properties` (рядок 33)

Замініть:
```properties
log.email.smtp.password=ВСТАВТЕ_ТУТ_APP_PASSWORD_З_GOOGLE
```

На ваш app password:
```properties
log.email.smtp.password=abcdefghijklmnop
```

---

## 🧪 Перевірка що Email Працює

### Запустіть тест:

```bash
javac -cp "libs/javax.mail-1.6.2.jar" -d bin -encoding UTF-8 -sourcepath src src/com/musicsystem/TestEmailAndLogging.java
java -cp "bin:libs/javax.mail-1.6.2.jar" com.musicsystem.TestEmailAndLogging
```

### Очікуваний результат:

```
✅ Тест завершено!
✓ Email з критичною помилкою відправлено на: vladisalvpro100igrok@gmail.com
```

### Перевірте пошту!

Має прийти лист з темою:
```
🚨 КРИТИЧНА ПОМИЛКА в Music System
```

---

## 📁 Структура Проекту

```
lab_5/
├── libs/
│   ├── javax.mail-1.6.2.jar        ← 📧 JavaMail тут
│   └── junit-platform-console-standalone-1.11.0.jar
├── logs/
│   └── application.log             ← 📝 Логи тут
├── resources/
│   └── logging.properties          ← ⚙️ Налаштування тут
├── run.sh                          ← 🚀 Запуск програми
├── compile.sh                      ← 📦 Компіляція
├── test_all.sh                     ← 🧪 Всі тести
└── src/
    └── com/musicsystem/...
```

---

## 💡 Корисні Команди

### Подивитись логи:
```bash
cat logs/application.log
tail -30 logs/application.log
```

### Очистити логи:
```bash
> logs/application.log
```

### Компіляція вручну:
```bash
javac -cp "libs/javax.mail-1.6.2.jar" -d bin -encoding UTF-8 -sourcepath src src/com/musicsystem/Main.java
```

### Запуск вручну:
```bash
java -cp "bin:libs/javax.mail-1.6.2.jar" com.musicsystem.Main
```

---

## ❓ Що Перевірити

### ✅ JavaMail встановлено?
```bash
ls -lh libs/javax.mail-1.6.2.jar
```

Має показати:
```
-rw-r--r--  1 admin  staff   644K Nov  8 00:10 libs/javax.mail-1.6.2.jar
```

### ✅ App password налаштовано?
```bash
grep "log.email.smtp.password" resources/logging.properties
```

Не має бути: `ВСТАВТЕ_ТУТ_APP_PASSWORD_З_GOOGLE`

### ✅ Логи працюють?
```bash
./run.sh
# Після запуску:
cat logs/application.log
```

---

## 🎉 Готово!

Тепер у вас працює:
- ✅ Логування у файл
- ✅ Email розсилка для FATAL помилок
- ✅ Юніт тести
- ✅ Повна документація

**Успіхів з проектом! 🚀**

---

## 📞 Швидка Довідка

| Команда | Призначення |
|---------|-------------|
| `./run.sh` | Запустити програму |
| `./compile.sh` | Тільки компіляція |
| `./test_all.sh` | Запустити всі тести |
| `cat logs/application.log` | Подивитись логи |

**Детальна інструкція з email:** `ІНСТРУКЦІЯ_EMAIL.md`

