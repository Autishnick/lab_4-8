# 📧 Налаштування Email для Логування

Ця система підтримує автоматичне відправлення критичних помилок на email. Для роботи цієї функції потрібна бібліотека JavaMail.

## 📦 Встановлення JavaMail API

### Варіант 1: Завантаження JAR файлів вручну

1. Завантажте бібліотеки:
   - **JavaMail API**: [javax.mail.jar](https://javaee.github.io/javamail/)
   - **JavaBeans Activation Framework**: activation.jar

2. Додайте до проекту в IntelliJ IDEA:
   - File → Project Structure (Ctrl+Alt+Shift+S)
   - Modules → Dependencies
   - Натисніть "+" → JARs or directories
   - Виберіть завантажені файли
   - Apply → OK

### Варіант 2: Maven (якщо використовується)

Додайте до `pom.xml`:

```xml
<dependencies>
    <dependency>
        <groupId>com.sun.mail</groupId>
        <artifactId>javax.mail</artifactId>
        <version>1.6.2</version>
    </dependency>
</dependencies>
```

### Варіант 3: Gradle (якщо використовується)

Додайте до `build.gradle`:

```gradle
dependencies {
    implementation 'com.sun.mail:javax.mail:1.6.2'
}
```

## ⚙️ Налаштування Email

Відредагуйте файл `resources/logging.properties`:

### 1. Gmail

```properties
log.email.on.fatal=true
log.email.to=admin@example.com
log.email.from=your-email@gmail.com

log.email.smtp.host=smtp.gmail.com
log.email.smtp.port=587
log.email.smtp.user=your-email@gmail.com
log.email.smtp.password=your-app-password
```

**⚠️ ВАЖЛИВО для Gmail**: Потрібно згенерувати App Password:
1. Перейдіть: https://myaccount.google.com/apppasswords
2. Оберіть "App" → "Other (Custom name)"
3. Введіть "Music System"
4. Скопіюйте згенерований пароль
5. Використайте його як `log.email.smtp.password`

### 2. Outlook/Hotmail

```properties
log.email.smtp.host=smtp-mail.outlook.com
log.email.smtp.port=587
log.email.smtp.user=your-email@outlook.com
log.email.smtp.password=your-password
```

### 3. Yahoo

```properties
log.email.smtp.host=smtp.mail.yahoo.com
log.email.smtp.port=587
log.email.smtp.user=your-email@yahoo.com
log.email.smtp.password=your-app-password
```

**Для Yahoo також потрібен App Password**: 
https://login.yahoo.com/account/security

### 4. Інші провайдери

Знайдіть SMTP налаштування вашого email провайдера:
- SMTP host (наприклад, smtp.example.com)
- SMTP port (зазвичай 587 для TLS або 465 для SSL)
- Ваш email та пароль

## 🧪 Тестування Email

### Вимкнення Email (для тестування)

Якщо не хочете налаштовувати email, просто вимкніть цю функцію:

```properties
log.email.on.fatal=false
```

Логи все одно будуть зберігатися у файл `logs/application.log`.

### Перевірка роботи Email

Для перевірки відправки email, можна штучно викликати фатальну помилку:
1. Запустіть програму
2. Виконайте дії, які можуть викликати критичну помилку
3. Або додайте тестовий код у Main.java:

```java
Logger.getInstance().fatal("TestClass", "Це тестове повідомлення для перевірки email");
```

## 📝 Структура Логування

### Рівні Логування

1. **DEBUG** - Детальна інформація для налагодження
2. **INFO** - Інформаційні повідомлення про роботу системи
3. **WARN** - Попередження про потенційні проблеми
4. **ERROR** - Помилки, які не критичні для роботи
5. **FATAL** - Критичні помилки (відправляються на email)

### Де зберігаються логи?

- **Файл**: `logs/application.log`
- **Консоль**: INFO та вище
- **Email**: Тільки FATAL помилки

### Приклад лог-запису

```
[2025-11-07 14:30:45] [INFO] [Main] Music System Management - Запуск програми
[2025-11-07 14:30:46] [INFO] [FileManager] ✓ Успішно завантажено 5 композицій з файлу: data/compositions.txt
[2025-11-07 14:32:15] [ERROR] [SaveToFileCommand] Помилка збереження файлу: Permission denied
[2025-11-07 14:35:00] [FATAL] [Main] Критична помилка програми: OutOfMemoryError
```

## 🔒 Безпека

**УВАГА!** Файл `logging.properties` містить конфіденційну інформацію (паролі).

### Рекомендації:

1. **НЕ додавайте** `logging.properties` до Git:
   ```bash
   echo "resources/logging.properties" >> .gitignore
   echo "src/logging.properties" >> .gitignore
   ```

2. Створіть шаблон `logging.properties.template`:
   ```properties
   log.email.smtp.user=YOUR_EMAIL_HERE
   log.email.smtp.password=YOUR_PASSWORD_HERE
   ```

3. Використовуйте змінні оточення (для production):
   ```java
   String password = System.getenv("EMAIL_PASSWORD");
   ```

## ❓ Усунення проблем

### Помилка: "Could not find or load main class javax.mail"

**Рішення**: JavaMail бібліотека не додана до classpath. Повторіть кроки встановлення.

### Помилка: "Authentication failed"

**Рішення**: 
- Перевірте логін та пароль
- Для Gmail використовуйте App Password, не звичайний пароль
- Перевірте, чи увімкнена двофакторна автентифікація

### Помилка: "Connection timed out"

**Рішення**:
- Перевірте інтернет з'єднання
- Переконайтесь, що firewall не блокує порт 587
- Спробуйте інший SMTP порт (465 для SSL)

### Email не відправляється, але програма працює

**Рішення**: Перевірте консоль на повідомлення:
```
Email налаштування не задані. Email не відправлено.
```

Це означає, що параметри email в `logging.properties` не налаштовані або налаштовані неправильно.

## 📞 Підтримка

Якщо виникли проблеми з налаштуванням:
1. Перевірте файл `logs/application.log` на деталі помилок
2. Переконайтесь, що всі налаштування в `logging.properties` правильні
3. Спробуйте спочатку вимкнути email (`log.email.on.fatal=false`) та переконатись, що логування у файл працює

---

**Примітка**: Email відправка виконується асинхронно в окремому потоці, тому не блокує роботу програми.

